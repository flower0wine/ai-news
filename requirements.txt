# AI News Lambda Function Dependencies
# Install with: pip install -r requirements.txt -t ./package

# LLM Integration (Litellm for unified LLM API)
# Supports 100+ LLM providers including Volcengine, OpenAI, Anthropic, etc.
litellm>=1.0.0

# HTML to Markdown conversion
# Lightweight library for converting HTML to Markdown
markdownify>=0.12.0

# Email sending via Resend
# Modern email API for developers
resend>=0.7.0

# AWS SDK for Python (boto3)
# Already available in Lambda runtime, but included for local testing
boto3>=1.26.0

# HTTP requests (for local development/testing)
requests>=2.28.0
