现在需要集成AI LLM API，利用 Litellm 来快速实现，LLM API 使用火山引擎。你需要熟悉如何集成，如何发起请求和接收响应，如何开启和关闭流式输出。

熟悉之后你需要进行对应的代码的封装，封装的要求大致为：代码的的模块化、可扩展性、并且具有易读性。

可扩展性指的是可以快速切换模型提供商，这个功能 litellm 本身具备，但是你的封装不能使用固定值，它应该是可配置的。并且API KEY 不能暴露，你需要使用 lambda 推荐的方式来解决。

封装好的 LLM API 是接下来的基础

# 任务

- 请求 https://alphasignal.ai/api/last-campaign （GET，无其它的参数）路径获取 JSON 数据，返回的数据格式如下：

```json
{
    "_id": "69a5a2661b0954916f671846",
    "html": "...",
    "subject": "🚨 OpenAI Pentagon Deal + Claude Memory Import Cross-Platform",
    "timestamp": "2026-03-02T14:44:53.899Z"
}
```

- 使用 python 依赖包实现 html 转 markdown，解析响应当中的 html 转换为 md。目的是减少传递给 LLM 的 token。
- 使用 LLM 进行页面的信息摘要，要求其对信息进行分析总结，并要求其输出符合新闻阅读的 HTML，并且在每个新闻的底部添加原文地址。
- 然后将 HTML 使用 resend 进行发送，相关的 resend 配置同样是可配置的，你需要理解为将消息发送给用户，使用resend可能只是其中的一个方式，你需要让其可扩展。

# 约束

- 在编写代码时使用符合 lambda 的最佳实践方式进行编写，对文件模块进行拆分以利于维护和理解
- 添加符合最佳实践的注释。
- 所有的依赖都必须要安装，不可使用动态导入。
- 在编写代码之前充分分析，一步一步实现，并逐步回顾之前的代码。